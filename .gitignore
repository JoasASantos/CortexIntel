# Rust build artifacts
/target
/gui/src-tauri/target
/gui/src-tauri/gen
**/*.rs.bk

# Generated / runtime output
/cortex-out
/cortex-demo
*.log

# Sample/mock data — not shipped; scaffold your own with `cortex init`
/demos

# Test & tooling artifacts — NEVER commit screenshots (may contain real data)
/.playwright-mcp
/*.png
/*.jpg
/*.jpeg
/*.pdf
/gui/src-tauri/app-icon.png
# (app icons under gui/src-tauri/icons/ are the generated logo and ARE tracked)
!/gui/src-tauri/icons/**

# OS / editor
.DS_Store
*.swp

# Never commit local accounts / secrets (these live in ~/.cortexintel anyway)
users.json
*.secret

# private reference docs — do not publish
DATA.md
DESIGN.md
exemplo-mapa.csv
